// "My Posts" and "Saved" used to live here as feed filters — both moved to
// the Profile screen (Posts/Media/Saved tabs) so they're not duplicated in
// two places. This bar now only filters by post category.
export const FILTERS = ["All posts", "Field notes", "Marketplace"];